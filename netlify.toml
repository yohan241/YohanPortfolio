[build]
  command = "rm -rf frontend/node_modules && npm --prefix frontend install && npm --prefix frontend run build"
  publish = "public/dist"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
